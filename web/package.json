{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base=/minimal-chess --outDir ../docs",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^16.3.1",
    "typescript": "^4.3.2",
    "vite": "^2.4.0"
  }
}
