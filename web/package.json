{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base=/minimal-chess/ --outDir ../docs --emptyOutDir",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^16.3.1",
    "sass": "^1.35.2",
    "typescript": "^4.3.2",
    "vite": "^2.9.16"
  }
}
